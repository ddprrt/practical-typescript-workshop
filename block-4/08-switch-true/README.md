# Exercise 8: `switch(true)`

Implement `handleResult` to check for successful operations/error cases. Make sure to check for `RangeError` and `TypeError`. Use a `switch(true)` block and instance of checks.
