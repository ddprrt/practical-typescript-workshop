// Implement literal type examples

// 1. Create literal types
type Direction = unknown;
type HttpMethod = unknown;

// 2. Implement a function using literal types
function move(direction: Direction, steps: number): void {
  // Implement
}

// 3. Create a type safe API request function
function request(method: HttpMethod, url: string): void {
  // Implement
}

export {};
