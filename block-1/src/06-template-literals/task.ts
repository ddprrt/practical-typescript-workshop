// Implement template literal type examples

// 1. Create a CSS unit type
type CSSUnit = unknown;

// 2. Create an event handler type
type EventHandler = unknown;

// 3. Implement a type-safe CSS function
function createCSSValue(value: number, unit: CSSUnit): string {
  // Implement
  return "";
}

// 4. Implement a type-safe event handler
function addEventListener(handler: EventHandler): void {
  // Implement
}

export {};
