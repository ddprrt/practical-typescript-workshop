// Implement conditional type examples

// 1. Create a type that extracts the return type of a function
type ReturnType<T> = unknown;

// 2. Create a type that extracts array element type
type ArrayElement<T> = unknown;

// 3. Create a type that makes all properties nullable
type Nullable<T> = unknown;

export {};
